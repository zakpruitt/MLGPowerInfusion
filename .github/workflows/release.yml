name: Build and release

on:
  push:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: release
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Version
        id: ver
        run: |
          # Major.minor comes from the .toc; the patch is the run number
          BASE=$(sed -nE 's/^## Version:[[:space:]]*([0-9]+\.[0-9]+).*/\1/p' MLGPowerInfusion.toc)
          INTERFACE=$(sed -nE 's/^## Interface:[[:space:]]*([0-9]+).*/\1/p' MLGPowerInfusion.toc | head -1)
          echo "version=${BASE}.${{ github.run_number }}" >> "$GITHUB_OUTPUT"
          echo "interface=${INTERFACE}" >> "$GITHUB_OUTPUT"

      - name: Package
        id: pkg
        run: |
          VERSION="${{ steps.ver.outputs.version }}"
          ZIP="MLGPowerInfusion-${VERSION}.zip"
          # Only what WoW loads; source art stays out of the zip
          rm -rf dist && mkdir -p dist/MLGPowerInfusion/Media
          cp MLGPowerInfusion.toc *.lua dist/MLGPowerInfusion/
          cp Media/*.tga Media/*.mp3 dist/MLGPowerInfusion/Media/
          sed -i -E "s/^## Version:.*/## Version: ${VERSION}/" dist/MLGPowerInfusion/MLGPowerInfusion.toc
          (cd dist && zip -qr "../${ZIP}" MLGPowerInfusion)
          # release.json is what WoWUp reads to pick the right asset for retail
          cat > release.json <<JSON
          {
            "releases": [
              {
                "name": "MLGPowerInfusion",
                "version": "${VERSION}",
                "filename": "${ZIP}",
                "nolib": false,
                "metadata": [
                  { "flavor": "mainline", "interface": ${{ steps.ver.outputs.interface }} }
                ]
              }
            ]
          }
          JSON
          echo "zip=${ZIP}" >> "$GITHUB_OUTPUT"

      - name: Release notes
        run: |
          PREV=$(git describe --tags --abbrev=0 2>/dev/null || true)
          {
            echo "Changes:"
            echo
            git log --no-merges --format='- %s' ${PREV:+${PREV}..}HEAD
            echo
            echo "Install or update with WoWUp: **Get Addons -> Install from URL** -> \`${{ github.server_url }}/${{ github.repository }}\`"
          } > notes.md

      - name: Publish
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "v${{ steps.ver.outputs.version }}" \
            "${{ steps.pkg.outputs.zip }}" release.json \
            --title "v${{ steps.ver.outputs.version }}" \
            --notes-file notes.md \
            --target "$(git rev-parse HEAD)"
